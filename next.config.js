/** @type {import('next').NextConfig} */
// next.config.js — Next.js configuration for IPL XI Predictor
const nextConfig = {
  images: {
    domains: ['lh3.googleusercontent.com'],
  },
}

module.exports = nextConfig
